# 方法

[English](METHODS.md) | **繁體中文**

[`README.zh-TW.md`](README.zh-TW.md) 的技術附錄。估的是什麼、怎麼估、這個設計能識別與不能識別什麼，以及那些數字在哪裡就不再是它們看起來的意思了。

---

## 1. 估的是什麼，以及怎麼估

這是兩件事，混在一起談正是第一版難以檢驗的原因。

**估計目標。** 對捕手 *c*，在他實際接到的那些球上，把接捕者從聯盟平均換成他之後，判好球機率的平均變化：

```
Δ_c = 在 c 的球上取平均 [ P(好球 | c 接) − P(好球 | 平均捕手接) ]
```

Δ 以每球定義，報告時換算成每 100 顆 shadow zone 球，或加總後乘以 0.125 runs 成為榜單數字。

有三個性質要緊。它是**相對於聯盟平均**定義的，不是相對於零——隨機效應模型只能把捕手效應識別到相差一個常數，全域水準會被截距吸收。它是在**該捕手自己的球**上取平均，所以面對不同球路組成的兩位捕手，各自以他實際看到的東西計分。它在機率尺度上，不是 logit 尺度。

**估計式。** 兩個，都在估 Δ：

- **未調整殘差**（v1 的做法）：`Δ̂_c = mean(實際 − 基準預測)`，在該捕手的球上取平均。不調整主審與投手。區間用二項式標準誤。
- **階層模型**：捕手、主審、投手三組交叉隨機截距放在殘差上，基準 logit 凍結為 offset。Δ 逐後驗抽樣計算後再摘要。

選 Δ 而不是 logit 尺度的係數 `u_catcher` 是刻意的：未調整殘差根本沒有 `u_catcher` 這個東西。用 `u` 去比較兩個估計式，就是在比較兩個不同的量。Δ 是兩者都在估的，也是榜單實際報告的數字，所以第 6 節的模擬檢驗的正是會被發表出去的那個數。

---

## 2. 資料與設計

2021–2023 例行賽，只取主審判定球：106 萬顆；剔除 |plate_x| > 2.5 英尺或標準化高度落在 [−1, 2] 之外的球以避免樣條外插後，剩 104 萬顆。資料來自 Statcast（`pybaseball`）；主審來自 MLB Stats API，以 `game_pk` join。高度標準化為 `(plate_z − sz_bot) / (sz_top − sz_bot)`。

### 2.1 基準機率一律樣本外

整個衡量建立在殘差「實際 − 預測」之上。如果基準模型是在它所評分的那些球上擬合的，殘差會被擬合本身壓平一部分。本專案每一顆球的基準機率都來自沒看過它的模型：

- **2021–2022**：五折 cross-fitting，folds 依 `game_pk` 分配。五次 GAM 擬合，每次預測自己沒看過的那一折。
- **2023**：由只在 2021–2022 訓練分割上擬合的模型預測。

兩種做法形式不同，但都是樣本外。

不這樣做的代價是可以量的。在 2023 上，同一個未調整估計式，用樣本內基準時對 Savant 的相關是 0.990，用樣本外基準是 0.958。樣本內版本與 Savant 共享結構——後者推測也是在季內擬合——相關因此被墊高。

### 2.2 依場次切分，不依單顆球

選模用的訓練／驗證切分是切在 `game_pk` 上，不是切在單顆球上。同一場的球共享主審、球場與那天的好球帶。切單顆球會讓相關的觀測同時出現在兩側，使驗證損失偏樂觀——那就失去切分的意義了。代價是比例無法剛好落在 20%，實際是 19.93%。

重抽也依同樣的道理：凡是用到 bootstrap 的地方，單位是場次而非單顆球。在 shadow zone 的殘差加總上，群集把標準誤放大 **1.21 到 1.29 倍**——比教科書警告暗示的小，但不可忽略。

### 2.3 Shadow zone

分析限定在基準模型給出 0.2 < p̂ < 0.8 的球。兩個理由，依重要性排序。

**實質理由**：judgement 有疑義的地方才輪得到 framing。正中好球帶的球，誰接都是好球。

**統計理由**：被丟掉的球幾乎不帶資訊。關於捕手效果的 Fisher information 正比於 p(1−p)，在兩端趨近於零。2023 的加總：

| | 佔球數 | 佔資訊量 |
|---|--:|--:|
| Shadow zone（0.2–0.8） | 14.5% | **60.8%** |

在 75 位判定球數 ≥1,000 的捕手上，捕手效果的標準誤平均放大 1.27 倍——不是球數比暗示的 2.6 倍。

**計算成本是第三個理由，而且是弱的那個。** 在相同迭代數下實測，NUTS 跑全季每次迭代的成本是跑 shadow zone 的 11.4 倍，也就是全量擬合大約一小時，並非做不到。這個限制真正買到的是**便宜的重複擬合**——兩季合併的擬合約七分鐘，而門檻敏感度分析、引擎對照與模擬全都需要擬合很多次，不是一次。

**循環定義**：shadow zone 是用基準模型定義的，所以必須先用**全部**判定球擬合基準模型，再回頭切子集。

**敏感度**：0.15/0.85 與 0.25/0.75 的結果一併報告。這一點連同「不去挑選讓區間最窄的門檻」的承諾，都在跑之前就寫下來了。

### 2.4 Holdout 紀律

2023 從一開始就被隔離。模型形式、shadow zone 門檻、推論引擎、估計目標定義、要報告哪些量，全部在 2021–2022 上定案。2023 在最後被使用一次，為的是讓新數字能與 v1 發表的 2023 表格在同一季上並排。

這是有代價的，代價也該講明：2023 被保留，跨季穩定性只剩一組年度配對，沒有 v1 報告過的那種衰減曲線。

---

## 3. 識別性

隨機效應處理的是小樣本不穩定，不是混淆。

### 3.1 捕手與主審分得乾淨

主審排班接近隨機，賽程交錯也夠密，兩組效應分得開。2023 shadow zone：

- 102 位捕手 × 94 位主審 = 9,588 個可能配對；**實際觀察到 3,811 個（39.7%）**
- 在 66 位 shadow zone 球數 ≥300 的捕手中，中位數接觸過 **50 位不同主審**；接觸最少的也有 27 位

這是密集的交錯。`u_catcher` 與 `v_umpire` 是可識別的。

### 3.2 捕手與投手分不開

混淆是單向的，而且方向和直覺相反。

- 一位捕手的球來自他最常搭配投手的比例：中位數 **14.9%**，第 90 百分位 22.4%。捕手接觸很多投手。
- 一位投手的球由他最常搭配捕手接走的比例：中位數 **60.5%**，第 90 百分位 **84.8%**。

一位投手的球大多由同一位捕手接。所以任何屬於該投手、卻沒有進模型的東西，都有一條乾淨的通道流進那位捕手的估計裡。

這件事直接顯示在後驗上。取每位合格捕手與他接得最多的那位投手，計算兩者效應在後驗抽樣之間的相關（66 對，排除池化組）：

| | ρ |
|---|--:|
| 中位數 | **−0.221** |
| 第 10 百分位 | −0.304 |
| 最負 | −0.374 |
| 低於 −0.2 的比例 | 63.6% |

負號是部分不可識別的特徵：在後驗之內，同一批判決的功勞在捕手與他的搭檔之間此消彼長。模型分不出是誰的，於是折衷，並讓兩邊的區間都變寬。

**所以捕手項是「在這個模型設定下與捕手相關的變異」，而投手這條通道是它不能被讀成技術的主要原因。** 第 6 節的模擬為這件事的代價給出數字。

### 3.3 收縮是什麼、不是什麼

部分池化處理的是「一位捕手只有 150 顆 shadow zone 球，他的原始估計很吵」。它不處理「他的球不成比例地來自兩位投手」。收縮讓小樣本估計不那麼誤判自己；它對一條系統性通道毫無作用。這兩種失效在榜單上長得很像，彼此卻毫無關係。

---

## 4. 估計

### 4.1 基準模型

```
logit P(好球) = te(plate_x, plate_z_std) + f(stand) + f(p_throws) + f(balls) + f(strikes)
```

Logistic GAM（pyGAM），張量樣條每個邊際 20 個 spline。選模看驗證集 log loss，不看 accuracy 也不看 AUC：framing 指標建立在機率值之上，所以要緊的是校準，而 AUC 對校準不敏感。

樣本內與樣本外的 log loss 相差 0.002（0.17346 對 0.17149）。410 個基底對上 55 萬列再加懲罰項，沒有過擬合的空間，所以 v1 報告樣本內指標是方法上的瑕疵，但沒有讓它的擬合指標失真。

真正的弱點是校準。在樣本外，擬合曲面在 p̂ = 0.5 以下高估好球、以上低估，在 10 萬顆留出球的六個分箱上單調偏移——一個真實的 S 形，來自轉換帶被平滑過頭。用 isotonic 重新校準之後，捕手的 framing runs 首尾移動 **0.32 runs**，而榜單全距是 28.5。真實，但低於會改變結論的程度。

### 4.2 階層模型

```
logit P(好球) = a + b·logit_base + u_catcher + u_umpire + u_pitcher
u_g = τ_g · z_g,    z_g ~ N(0, 1),    τ_g ~ HalfNormal(0.5)
```

兩階段：基準 logit 凍結為 offset，隨機效應競爭殘差。單季球數少於 100 的投手併成一組；他們的個別效應無論如何都會被收縮到接近零，而不併的話 level 數無法處理。

**非中心參數化不是選配。** τ 小的時候，中心化形式的後驗呈漏斗形，NUTS 會產生 divergence。本專案從一開始就寫成非中心，所有擬合的 divergence 都是零或接近零（模擬研究的 80 萬次抽樣中共 2 個）。

**換引擎什麼都沒改變，而這正是發現。** 在完全相同的資料上，變分貝葉斯與 NUTS 對每位捕手效應的相關是 r = 0.9999（Spearman 0.9998），VB 的後驗標準差是 NUTS 的 0.95 倍。「變分推論會低估後驗變異」這個理論警告方向正確，在這裡值 5%。NUTS 提供的是後驗**樣本**，而成對比較機率與涵蓋率檢驗需要它，VB 給不了。

---

## 5. 不確定性

**Δ 的後驗。** 對每一個後驗抽樣，重建每顆球的 η，以 `σ(η) − σ(η − u_catcher)` 計算 Δ，再依捕手取平均。結果是每位捕手一個 Δ 的後驗分布，區間取其 2.5／97.5 百分位。

**Runs。** `runs = Δ × shadow zone 球數 × 0.125` **逐抽樣**計算，再摘要。先把點估計乘一乘、事後再配一個區間，會低估不確定性。

**成對比較。** `P(Δ_A > Δ_B)` 直接從聯合後驗抽樣數出來。要回答「A 是不是比 B 好」而不假裝兩個估計獨立，只有這個辦法。

**可分辨性。** 2023：102 位捕手中 27 位（26%）的區間不含零；27% 的配對在 95% 水準下分得出勝負。限制在 shadow zone 球數 ≥1,000 的捕手時，兩個數字升到 53% 與 55%。在 2021–2022 上，P(第 1 名勝第 2 名) = 0.77。

**兩端的涵蓋率低於名目值。** 模擬顯示效果最大的三分之一，其涵蓋率比整體低 3 到 6 個百分點，每一個情境都是，包括沒有混淆的基準情境——那裡是 89.0% 對 94.7%。做這件事的是估計式而非資料：部分池化的穩定性是用尾端換來的，而尾端正是榜單被閱讀的地方。caterpillar 圖以圖說帶著這條警告。

---

## 6. 模擬設計

模擬是本專案唯一真值由設定而非推論得來的檢查。它的結論在 README；本節講的是它怎麼建，以及它不能證明什麼。

### 6.1 規模

合成資料用 **30 位捕手 × 每人約 500 球**，約 15,000 列，而真實分析集是 5 萬到 10 萬列。這是計算成本的取捨而非統計上的選擇：這個規模下一次擬合 9 秒，800 次才做得完。**涵蓋率取決於樣本量，所以這些數字不能當成整季樣本量下的精確值。**

球位機率與捕手出賽量的不均勻程度都從真實的 2022 shadow zone 抽，而不是從一個方便的參數化分布抽。現實中的不均——主戰比替補多一個數量級——正是要檢驗的東西之一。

### 6.2 情境

八個，分兩組。前四個操弄資料結構，後四個攻擊模型假設。

| 情境 | 操弄 |
|---|---|
| baseline | 主審隨機分派 |
| unequal | 球數從 30 到 1,561 |
| umpire_confound | 部分捕手系統性遇到寬鬆主審 |
| battery | 投捕配對集中 |
| location_shared | 捕手效果隨球位變化，球位與捕手獨立 |
| location_mix | 同上，但各捕手面對不同的球位分布 |
| heavy_tail | 捕手效果取自 t(3)，違反常態先驗 |
| omitted_covariate | 一個影響判決、與捕手相關、不在模型裡的變數 |

**`location_shared` 是一個刻意保留的失敗。** 它看起來像設錯，其實不是：當球位獨立於捕手抽樣時，每位捕手面對的分布相同，交互項平均之後就是常數 `γ_c · E[centred]`，常數截距模型完全還原得了。這個情境下捕手層級的平均球位標準差是 0.017，而 `location_mix` 是 0.078。它留在表上，當作它後來變成的那個對照組。

在 `omitted_covariate` 成功之前，另外兩個情境設計也失敗了，原因每次都一樣：**擬合的模型把原本要打壞它的東西吸收掉了。** 定義在投手層級的變數，會被投手隨機效果整包吸收。逐球雜訊與捕手無關，只是增加未解釋變異。只有帶**捕手層級成分**的變數，加上把那份成分排除在真值之外，才會咬。

最後那個構造接近套套邏輯：把資料分不開的東西從真值裡拿掉，涵蓋率必然失敗。它仍然被納入，因為有用的是失敗的**幅度**，也因為它把一件事講明了——其他情境的涵蓋率漂亮，並不授權做因果解讀。

### 6.3 指標

bias、RMSE、95% 區間涵蓋率、排名還原度（對真實排序的 Spearman），兩個估計式都算，每情境 100 次重複。涵蓋率的蒙地卡羅誤差為 ±1.1%。

**涵蓋率另外依效果大小分層報告。** 重尾情境下，30 位捕手裡只有一兩位是離群者，兩位都射失也只讓整體涵蓋率移動六個百分點，會消失在平均裡。失效在尾端，整體數字把它藏起來。

### 6.4 混淆強度掃描

與其挑一個混淆大小、回報它有沒有打壞，這裡把與捕手相關的混淆強度從零掃到真實捕手效果的兩倍，每個點 50 次重複。這把「未觀測混淆重不重要」——一個答案取決於任意選擇的問題——轉換成「要多大才重要」，後者沒有這個問題。

### 6.5 模擬不能證明什麼

八個情境裡有五個的生成過程就是模型自己的函數形式。那裡的涵蓋率好看是幾乎必然的，不應該被讀成驗證。三個為了打壞模型而建的情境沒有打壞它，這界定了主張的範圍：這些區間撐得住**此處所能構造出來的每一種設錯**，不是撐得住每一種設錯。

---

## 7. 這裡的數字能界定什麼

完整的限制清單在 [`README.zh-TW.md`](README.zh-TW.md)。其中三條可以給出數字，本節
就是為那些數字而寫。其餘是範圍陳述，除了 README 已寫的之外沒有可補充的：未建模的
球路特徵、固定的 run value、座標剔除。

**捕手與投手部分不可分離。** 一位投手的球由他最常搭配的捕手接走的比例中位數是
60.5%，第 90 百分位 84.8%。捕手效應與其最常搭配投手效應的後驗相關中位數
ρ = −0.221，63.6% 的配對低於 −0.2。捕手項是關聯而非技術，而這就是造成它如此的通道。

**兩端的涵蓋率約 88% 而非 95%**——每一個模擬情境皆然，包括沒有混淆的基準情境。
造成它的是收縮，所以這是估計式的性質而不是這份資料的問題，而且它作用的位置正好是
榜單被閱讀的位置。

**與捕手相關的未觀測混淆，其代價是可以量的**：沒有混淆時 94%，與被測效果等大時
88%，兩倍時 66%。第 3.2 節確立了這個結構存在於真實資料中。資料本身無法界定真實分析
落在那條曲線的哪個位置，而且再好的推論方法也改變不了這件事。它是設計的性質，不是估計的性質。

---

## 8. 踩過的坑

記錄下來是因為每一個都花了時間，而且每一個如果沒被發現，都會產生一個錯的數字。

**單一驗證分割不足以偵測這個量級的偏誤。** 一組 971 場的分割顯示 shadow zone 殘差有偏，三個門檻都達到 p ≈ 0.03。用全部 4,856 場做五折 cross-fitting 之後是 z = +0.30，蓋住零。前一個結果是假陽性——拿一個 5% 的檢定跑三個高度相關的門檻。這同時也是對 v1 其他單一分割數字的警告。

**量 JAX 程式的時間而不 block，量到的是派工不是計算。** `mcmc.run()` 在樣本存在之前就回傳，它們在第一次被存取時才實體化。停錶之前必須 `jax.block_until_ready(mcmc.get_samples())`。

**`numpyro.set_host_device_count` 在 XLA 初始化之後會被忽略。** 每次擬合都呼叫它，會讓後面的 4 鏈執行安靜地退化成循序執行，只在 stderr 留一行警告，wall time 加倍。裝置數必須在 import JAX 之前透過 `XLA_FLAGS` 設定。

**相對於零估計的效應與相對於聯盟平均估計的效應差一個常數**，在模擬裡會表現成一個跨所有情境完全相同的小偏誤。五個結構完全不同的情境回報同一個偏誤到小數第四位，那是算術，不是統計性質。

**arviz 1.3.0 與 numpyro 0.21.0 不相容**；`az.from_numpyro` 會在 `infer_dims` 內失敗。本專案的診斷直接使用 `numpyro.diagnostics`。

---

## 9. 重現性

指令序列見 [`README.zh-TW.md`](README.zh-TW.md)。v1 的流程未變動且仍可執行，其指令在 [`v1.0`](../../tree/v1.0) tag。

種子固定：切分與 cross-fitting folds 用 `20260906`，模擬抽樣用重複次數的索引。資料檔、模型快取與模擬輸出不進版控，由文件所列指令重新產生。
